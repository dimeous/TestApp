import { StackNavigator } from 'react-navigation';
// Components
import { Main, ShowPhoto } from './app/views';


export default App = StackNavigator({
  Main:      { screen: Main },
  ShowPhoto: { screen: ShowPhoto },
});
